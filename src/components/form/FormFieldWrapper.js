import styled from '@emotion/styled';
import css from '@styled-system/css';

const FormFieldWrapper = styled('div')(css({
  padding: '8px 0',
  width: '100%',
  boxSizing: 'border-box',
}));

export default FormFieldWrapper;
